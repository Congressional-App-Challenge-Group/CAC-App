import{z}from"zod";import{roles,statuses}from"@/types";export const preferencesSchema=z.object({zipCode:z.string().regex(/^\d{5}$/),roles:z.array(z.enum(roles)).min(1),interests:z.array(z.string()).min(3)});export const decisionSchema=z.object({title:z.string().min(8),slug:z.string().regex(/^[a-z0-9-]+$/),organization:z.enum(["charlotte","mecklenburg-county","cms"]),topic:z.string().min(2),status:z.enum(statuses),summary:z.string().min(20),whyItMatters:z.string().min(20),affectedZipCodes:z.array(z.string().regex(/^\d{5}$/)),affectedRoles:z.array(z.enum(roles)),isPublished:z.boolean()});
