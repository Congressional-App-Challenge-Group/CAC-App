import type { Organization } from "@/types";

export const orgNames: Record<Organization, string> = {
  charlotte: "Charlotte",
  "mecklenburg-county": "Mecklenburg County",
  cms: "CMS",
};

export const ACTIVE_APPROVAL_DAYS = 60;
