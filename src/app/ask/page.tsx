import{AskBox}from"@/features/assistant/ask-box";import{decisions}from"@/data/decisions";import Link from"next/link";export default function Ask(){return <div className="shell max-w-3xl py-10"><p className="eyebrow">Source-grounded answers</p><h1 className="mt-2 text-4xl font-black">Ask CivicLens</h1><p className="mt-3 text-slate-600">This MVP uses deterministic demonstration responses. Choose a tracked decision for detailed questions.</p><div className="mt-8"><AskBox/></div><h2 className="mt-10 text-xl font-black">Choose a decision</h2><div className="mt-3 grid gap-2">{decisions.slice(0,6).map(d=><Link className="card flex items-center justify-between p-4 font-bold hover:border-civic" href={`/decisions/${d.slug}#ask`} key={d.id}>{d.title}<span className="text-civic">Ask →</span></Link>)}</div></div>}
