import{notFound}from"next/navigation";import{getDecision}from"@/data/decisions";import{DecisionForm}from"@/features/admin/decision-form";export default async function Edit({params}:{params:Promise<{id:string}>}){const{id}=await params;const d=getDecision(id);if(!d)notFound();return <><h1 className="text-3xl font-black">Edit decision</h1><p className="mt-2 text-slate-600">Update status, affected groups, sources, and editorial copy.</p><DecisionForm decision={d}/></>}
