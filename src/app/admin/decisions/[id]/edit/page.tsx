import{notFound}from"next/navigation";import{getDecision}from"@/server/decisions";import{DecisionForm}from"@/features/admin/decision-form";export default async function Edit({params}:{params:Promise<{id:string}>}){const{id}=await params;const d=await getDecision(id);if(!d)notFound();return <><h1 className="text-3xl font-black">Review decision</h1><p className="mt-2 text-slate-600">Secure write access is required before changes can be persisted.</p><DecisionForm decision={d}/></>}
