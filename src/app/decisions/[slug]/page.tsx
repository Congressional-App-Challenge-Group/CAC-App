import{notFound}from"next/navigation";import Link from"next/link";import{ArrowLeft,CalendarDays,ExternalLink,MapPin,Users}from"lucide-react";import{decisions,getDecision,orgNames}from"@/data/decisions";import{Timeline}from"@/features/timeline/timeline";import{DecisionCard}from"@/features/decisions/decision-card";import{AskBox}from"@/features/assistant/ask-box";
export function generateStaticParams(){return decisions.map(d=>({slug:d.slug}))}export default async function Detail({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const d=getDecision(slug);if(!d)notFound();return <div className="shell py-8"><Link href="/explore" className="inline-flex items-center gap-1 text-sm font-bold text-civic"><ArrowLeft size={16}/> Back to explore</Link><div className="mt-7 grid gap-8 lg:grid-cols-[1fr_340px]"><article><div className="flex flex-wrap gap-2"><span className="eyebrow">{orgNames[d.organization]} · {d.topic}</span><span className="status" data-status={d.status}>{d.status}</span></div><h1 className="mt-3 max-w-4xl text-4xl font-black leading-tight md:text-5xl">{d.title}</h1><p className="mt-5 text-xl leading-8 text-slate-700">{d.summary}</p><div className="mt-8 grid gap-4 sm:grid-cols-3"><Info icon={<Users/>} title="Who may be affected" text={d.affectedRoles.join(", ")}/><Info icon={<MapPin/>} title="Areas involved" text={d.affectedAreas.join(", ")}/><Info icon={<CalendarDays/>} title="Next important date" text={`${d.importantDates[0].date} · ${d.importantDates[0].label}`}/></div><Section title="Why it matters"><p>{d.whyItMatters}</p></Section><Section title="Latest official update"><p>{d.latestUpdate}</p><p className="mt-3"><b>Next expected step:</b> {d.nextStep}</p></Section><Section title="Before and after"><div className="grid gap-4 md:grid-cols-2"><div className="rounded-xl bg-slate-100 p-4"><p className="eyebrow">Before</p><p className="mt-2">{d.beforeText}</p></div><div className="rounded-xl bg-mint p-4"><p className="eyebrow">After</p><p className="mt-2">{d.afterText}</p></div></div></Section><Section title="Decision timeline"><Timeline events={d.timeline}/></Section><Section title="Official sources"><ul className="space-y-3">{d.sources.map(s=><li className="card p-4" key={s.id}><a href={s.url} className="flex items-center justify-between gap-3 font-bold text-civic">{s.title}<ExternalLink size={17}/></a><p className="mt-1 text-sm text-slate-500">{s.organization} · {s.pageReference} · {s.verificationStatus}</p></li>)}</ul></Section><AskBox decision={d}/></article><aside className="space-y-4"><DecisionCard decision={d} reason="You opened this decision"/><div className="rounded-xl border border-amber bg-[#fff7e6] p-4 text-sm"><b>Demonstration only.</b> This fictional item does not describe a current government decision.</div></aside></div></div>}
function Info({icon,title,text}:{icon:React.ReactNode;title:string;text:string}){return <div className="card p-4"><span className="text-civic">{icon}</span><h2 className="mt-2 text-sm font-black">{title}</h2><p className="mt-1 text-sm text-slate-600">{text}</p></div>}function Section({title,children}:{title:string;children:React.ReactNode}){return <section className="mt-10"><h2 className="mb-4 text-2xl font-black">{title}</h2><div className="text-base leading-7 text-slate-700">{children}</div></section>}
