import type {Metadata,Viewport} from "next";import "./globals.css";import {Analytics} from "@vercel/analytics/next";import {Header} from "@/components/layout/nav";import {PreferencesProvider} from "@/features/preferences/preferences";import{PwaRegister}from"@/components/pwa-register";import{DecisionsProvider}from"@/components/decisions-provider";import{getDecisions}from"@/server/decisions";
export const metadata:Metadata={title:{default:"CivicLens",template:"%s · CivicLens"},description:"Know what is changing before it affects you.",manifest:"/manifest.webmanifest"};
export const viewport:Viewport={themeColor:"#17362f",width:"device-width",initialScale:1};
export const dynamic="force-dynamic";
const themeScript=`(()=>{try{const saved=localStorage.getItem('civiclens-theme');const theme=saved==='light'||saved==='dark'?saved:matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light';document.documentElement.dataset.theme=theme;document.documentElement.style.colorScheme=theme}catch{}})()`;
export default async function RootLayout({children}:{children:React.ReactNode}){const decisions=await getDecisions();return <html lang="en" suppressHydrationWarning><head><script dangerouslySetInnerHTML={{__html:themeScript}}/></head><body><DecisionsProvider decisions={decisions}><PreferencesProvider><PwaRegister/><Header/><main>{children}</main></PreferencesProvider></DecisionsProvider><Analytics/></body></html>}
