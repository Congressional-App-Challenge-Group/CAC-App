"use client";
import{createContext,useContext,useEffect,useState}from"react";import type{UserPreferences}from"@/types";
const defaults:UserPreferences={zipCode:"28205",roles:["Other resident"],interests:["Public transit","Housing","Parks"]};
type Ctx={preferences:UserPreferences;setPreferences:(p:UserPreferences)=>void;following:string[];toggleFollow:(id:string)=>void;ready:boolean};
const PreferencesContext=createContext<Ctx|null>(null);
export function PreferencesProvider({children}:{children:React.ReactNode}){const[preferences,setP]=useState(defaults);const[following,setFollowing]=useState<string[]>([]);const[ready,setReady]=useState(false);useEffect(()=>{try{const p=localStorage.getItem("civiclens-preferences"),f=localStorage.getItem("civiclens-following");if(p)setP(JSON.parse(p));if(f)setFollowing(JSON.parse(f))}catch{localStorage.removeItem("civiclens-preferences");localStorage.removeItem("civiclens-following")}finally{setReady(true)}},[]);const setPreferences=(p:UserPreferences)=>{setP(p);safeSet("civiclens-preferences",p)};const toggleFollow=(id:string)=>setFollowing(old=>{const next=old.includes(id)?old.filter(x=>x!==id):[...old,id];safeSet("civiclens-following",next);return next});return <PreferencesContext.Provider value={{preferences,setPreferences,following,toggleFollow,ready}}>{children}</PreferencesContext.Provider>}
export const usePreferences=()=>{const value=useContext(PreferencesContext);if(!value)throw new Error("PreferencesProvider missing");return value};
function safeSet(key:string,value:unknown){try{localStorage.setItem(key,JSON.stringify(value))}catch{}}
