import type { DecisionStatus } from "@/types";

export const slugify=(value:string)=>value.toLowerCase().normalize("NFKD").replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"").slice(0,80);
export function plain(value:unknown){return String(value||"").replace(/<[^>]*>/g," ").replace(/&nbsp;|&#160;/g," ").replace(/&amp;/g,"&").replace(/\s+/g," ").trim()}
export function statusFromAction(action:unknown,passed:unknown,date:string):DecisionStatus{const value=plain(action).toLowerCase();if(passed===1||/adopted|approved|passed/.test(value))return"Approved";if(/denied|rejected|failed/.test(value))return"Rejected";if(/continued|postponed|tabled/.test(value))return"Delayed";return new Date(date)>new Date()?"Scheduled for vote":"Under review"}
export function topicFromText(value:string){const v=value.toLowerCase();if(/rezon|zoning|development|housing/.test(v))return"Development and rezoning";if(/school|student|education/.test(v))return"Education";if(/transit|bus|rail/.test(v))return"Public transit";if(/budget|appropriat|fund|contract/.test(v))return"Government budgets";if(/road|street|traffic|transport/.test(v))return"Roads";if(/park|greenway/.test(v))return"Parks";if(/health/.test(v))return"Public health";if(/police|fire|safety/.test(v))return"Public safety";return"Local government"}
export function importance(value:string){const v=value.toLowerCase();let n=0;if(/ordinance|resolution|policy|adopt|approve|vote/.test(v))n+=4;if(/budget|million|funding|contract/.test(v))n+=3;if(/public hearing|rezon|housing|school|transit|tax|safety/.test(v))n+=3;if(/ceremon|recognition|minutes|invocation|pledge|adjourn/.test(v))n-=5;return n}
